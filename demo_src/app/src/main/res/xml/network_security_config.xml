<network-security-config>
    <debug-overrides>
        <trust-anchors>
            <!-- Trust user added CAs while debuggable only -->
            <certificates src="@raw/charles" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>